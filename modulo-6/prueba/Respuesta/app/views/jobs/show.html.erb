<div class="row py-5">
    <h2 class="text-center">Aplicaciones</h2>
</div>

<div class="row py-5">
    <% @applies.each do |apply| %>
        <div class="col-12">
            <h4 class="text-center">Aplicación #<%= apply.id %></h4>
            <p>Trabajo: <%= @job.title %> </p>
            <p>Postulante: <%= apply.user.name.to_s %></p>
            <p>Estado: <%= apply.status %></p>
            <p>Justificación para contratación: <%= apply.application %></p>
            <%= link_to 'Modifica el estado de la postulación', edit_apply_path(apply.id), class: 'btn btn-primary w-100' %>
        </div>
    <% end %>
</div>

<div class="row py-5">
    <%= link_to "Vuelve", root_path %>
</div>